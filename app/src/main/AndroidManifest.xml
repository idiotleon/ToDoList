<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tek.first.servant">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".todolist.activity.ToDoListDisplayActivity"
            android:label="@string/app_name"
            android:uiOptions="splitActionBarWhenNarrow"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".todolist.activity.ToDoItemDetailsActivity" />
        <activity
            android:name=".todolist.activity.ToDoListPreferenceActivity"
            android:label="ToDoList User Preference">
            <intent-filter>
                <action android:name="com.example.tek.first.servant.ACTION_USER_PREFERENCE" />
            </intent-filter>
        </activity>
    </application>

</manifest>
